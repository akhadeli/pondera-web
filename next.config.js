/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        reactStrictMode: false,
        appDir: true,
        serverActions: true,
    }
}

module.exports = nextConfig
